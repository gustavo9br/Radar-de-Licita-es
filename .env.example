# Usado só pelo docker-compose.dev.yml, pra passar o mesmo APP_TOKEN do backend/.env
# como build-arg do frontend (VITE_APP_TOKEN é embutido no bundle em tempo de build).
# Copie pra ".env" e use o mesmo valor de APP_TOKEN definido em backend/.env.
APP_TOKEN=troque-por-um-valor-secreto
